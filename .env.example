DATABASE_URL="postgresql://usuario:password@host/basededatos?sslmode=require"
APP_SECRET="una-cadena-larga-y-aleatoria-para-firmar-la-sesion"

# Opcional: si no la defines, simplemente no se envían correos de asignación.
RESEND_API_KEY="re_xxxxxxxx"
# Opcional: remitente de los correos. Si no lo defines, usa el dominio de
# pruebas de Resend (onboarding@resend.dev), que no requiere verificar dominio.
RESEND_FROM_EMAIL="Yorked Studio <onboarding@resend.dev>"
